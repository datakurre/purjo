[project]
name = "e2e-hello-example"
version = "0.1.0"
description = "e2e fixture: happy path through the `pur init` scaffolding"
requires-python = ">=3.12"
dependencies = [
    "robotframework>=7.4.1",
]

[tool.purjo.topics."My Topic in BPMN"]
name = "My Test in Robot"
on-fail = "ERROR"
process-variables = true
